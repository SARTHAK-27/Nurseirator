import 'package:flutter/material.dart';
class booked extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Flexible(
        child: Container(
          child: Text("Congratulations your booking is done you will get the mail Shortly"),
        ),
      ),
    );
  }
}
